apiVersion: v1
kind: Pod
metadata:
  name: pod1
spec:
  restartPolicy: OnFailure
  containers:
    - image: gcr.io/jarvice-apps/k8s-rdma-device:1.0
      name: pod1-ctr
      command: ["sleep"]
      args: ["100000"]

      resources:
        limits:
          jarvice/ibrdma-device: 1